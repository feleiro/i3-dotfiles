# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status

## Audio: Raise volume
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
## Audio: Lower volume
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
## Audio: Mute/unmute speaker
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
## Audio: Mute/unmute microphone
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# move tiling windows via drag & drop by left-clicking into the title bar,
# or left-clicking anywhere into the window while holding the floating modifier.
tiling_drag modifier titlebar

## Apps: Open terminal (Alacritty)
bindsym $mod+Return exec i3-sensible-terminal

## Apps: Open file manager (Nemo)
bindsym $mod+e exec dolphin

## Window: Kill focused window
bindsym $mod+q kill

## Apps: Open application launcher (rofi)
bindcode $mod+40 exec "rofi -modi drun,run -show drun"

# change focus
## Focus: Move focus left
bindsym $mod+j focus left
## Focus: Move focus down
bindsym $mod+k focus down
## Focus: Move focus up
bindsym $mod+l focus up
## Focus: Move focus right
bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
## Focus: Move focus left (arrows)
bindsym $mod+Left focus left
## Focus: Move focus down (arrows)
bindsym $mod+Down focus down
## Focus: Move focus up (arrows)
bindsym $mod+Up focus up
## Focus: Move focus right (arrows)
bindsym $mod+Right focus right

# move focused window
## Window: Move window left
bindsym $mod+Shift+j move left
## Window: Move window down
bindsym $mod+Shift+k move down
## Window: Move window up
bindsym $mod+Shift+l move up
## Window: Move window right
bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
## Window: Move window left (arrows)
bindsym $mod+Shift+Left move left
## Window: Move window down (arrows)
bindsym $mod+Shift+Down move down
## Window: Move window up (arrows)
bindsym $mod+Shift+Up move up
## Window: Move window right (arrows)
bindsym $mod+Shift+Right move right

# split in horizontal orientation
## Layout: Split horizontally
bindsym $mod+h split h

# split in vertical orientation
## Layout: Split vertically
bindsym $mod+v split v

# enter fullscreen mode for the focused container
## Window: Toggle fullscreen
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
## Layout: Stacked layout
bindsym $mod+Shift+s layout stacking
## Layout: Tabbed layout
bindsym $mod+Shift+w layout tabbed
## Layout: Toggle split layout
bindsym $mod+Shift+e layout toggle split

# toggle tiling / floating
## Window: Toggle floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
## Focus: Toggle focus between tiling and floating windows
bindsym $mod+Control+space focus mode_toggle

# focus the parent container
## Focus: Parent container
bindsym $mod+a focus parent

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
## Workspace: Switch to workspace 1
bindsym $mod+1 workspace number $ws1
## Workspace: Switch to workspace 2
bindsym $mod+2 workspace number $ws2
## Workspace: Switch to workspace 3
bindsym $mod+3 workspace number $ws3
## Workspace: Switch to workspace 4
bindsym $mod+4 workspace number $ws4
## Workspace: Switch to workspace 5
bindsym $mod+5 workspace number $ws5
## Workspace: Switch to workspace 6
bindsym $mod+6 workspace number $ws6
## Workspace: Switch to workspace 7
bindsym $mod+7 workspace number $ws7
## Workspace: Switch to workspace 8
bindsym $mod+8 workspace number $ws8
## Workspace: Switch to workspace 9
bindsym $mod+9 workspace number $ws9
## Workspace: Switch to workspace 10
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
## Workspace: Move window to workspace 1
bindsym $mod+Shift+1 move container to workspace number $ws1
## Workspace: Move window to workspace 2
bindsym $mod+Shift+2 move container to workspace number $ws2
## Workspace: Move window to workspace 3
bindsym $mod+Shift+3 move container to workspace number $ws3
## Workspace: Move window to workspace 4
bindsym $mod+Shift+4 move container to workspace number $ws4
## Workspace: Move window to workspace 5
bindsym $mod+Shift+5 move container to workspace number $ws5
## Workspace: Move window to workspace 6
bindsym $mod+Shift+6 move container to workspace number $ws6
## Workspace: Move window to workspace 7
bindsym $mod+Shift+7 move container to workspace number $ws7
## Workspace: Move window to workspace 8
bindsym $mod+Shift+8 move container to workspace number $ws8
## Workspace: Move window to workspace 9
bindsym $mod+Shift+9 move container to workspace number $ws9
## Workspace: Move window to workspace 10
bindsym $mod+Shift+0 move container to workspace number $ws10

# reload the configuration file
## Config: Reload i3 config
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
## Config: Restart i3
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
## Session: Exit i3 (with confirmation)
bindsym $mod+m exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        ## Resize: Shrink width (j)
        bindsym j resize shrink width 10 px or 10 ppt
        ## Resize: Grow height (k)
        bindsym k resize grow height 10 px or 10 ppt
        ## Resize: Shrink height (l)
        bindsym l resize shrink height 10 px or 10 ppt
        ## Resize: Grow width (semicolon)
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        ## Resize: Shrink width (Left)
        bindsym Left resize shrink width 10 px or 10 ppt
        ## Resize: Grow height (Down)
        bindsym Down resize grow height 10 px or 10 ppt
        ## Resize: Shrink height (Up)
        bindsym Up resize shrink height 10 px or 10 ppt
        ## Resize: Grow width (Right)
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

## Screen: Take screenshot of selected area and copy to clipboard
bindsym $mod+Print exec --no-startup-id scrot -s /tmp/screenshot.png && xclip -selection clipboard -t image/png -i /tmp/screenshot.png && rm /tmp/screenshot.png

## Wallpaper: Select wallpaper (custom script)
bindsym $mod+Control+t exec "~/.config/i3/config.d/custom.d/wallpaper_selection.sh"

## Apps: Enable resize mode
bindsym $mod+r mode "resize"

## Other: Open terminal in config directory
bindsym $mod+F1 exec alacritty --working-directory ~/.config/i3/config.d

## Other: Open keybind cheatsheet
bindsym $mod+slash exec "~/.config/i3/config.d/custom.d/cheatsheet.py"
